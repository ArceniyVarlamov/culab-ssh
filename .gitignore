__pycache__/
*.pyc
.DS_Store

# Артефакты пушей (fallback архивы, большие тарболы)
*.tgz
*.tar
*.tar.gz

# Личные шорткаты с локальными путями (не публикуем)
push-*
apply-*

# code-server (отдельный сетап, к этому проекту не относится)
code-server/
